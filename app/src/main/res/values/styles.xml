<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Status bar stuff. -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

        <!-- Font Family changing -->
        <item name="android:fontFamily">@font/product_sans</item>
    </style>

    <style name="AppTheme.CustomNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Status bar stuff. -->
        <!--        <item name="android:statusBarColor">@color/colorPrimaryDark</item>-->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

        <!-- Font Family changing -->
        <item name="android:fontFamily">@font/product_sans</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextReportItemTitle">
        <item name="android:layout_margin">@dimen/_2sdp</item>
        <item name="android:textSize">@dimen/_12sdp</item>
    </style>

    <style name="TextReportItemData">
        <item name="android:layout_margin">@dimen/_2sdp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#000</item>
        <item name="android:textSize">@dimen/_12sdp</item>
    </style>

</resources>
